# Changesets

Run `pnpm changeset` from the repo root to add a changeset describing your
change. The Release GitHub Action consumes pending changesets to bump
versions and publish to npm. See `MIGRATION.md` and `CONTRIBUTING.md` for
the full workflow.
