{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "noEmit": true,
    "allowSyntheticDefaultImports": true,
    // `import.meta.env.BASE_URL` in ReplayView — resolves the fixture URL
    // against the GitHub Pages base path.
    "types": ["vite/client"]
  },
  "include": ["src/**/*"]
}
